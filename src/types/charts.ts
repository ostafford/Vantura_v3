/**
 * Shared chart data types used by dashboard bar charts (Insights).
 */

export type InsightsChartDatum = {
  category_id: string
  name: string
  totalDollars: number
  fill: string
  stroke: string
}
